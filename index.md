<style>
:root{ --bg:#0b0b0b; --fg:#e7e7e7; --muted:#b6b6b6; --border:rgba(255,255,255,.12); --accent:#7fd1ff; --accent-2:#ffd580; --code:#1a1a1a; }
html{ color-scheme: dark; }
body { background:var(--bg); color:var(--fg); }
a{ color:var(--accent); } a:hover{ color:var(--accent-2); }
.site-header{ background:#000; border-bottom:1px solid var(--border); }
.site-title, .site-title:visited{ color:var(--fg); }
.site-nav .page-link{ color:var(--fg); }
h1,h2,h3,h4,h5,h6{ color:var(--fg); }
p,li{ color:var(--fg); opacity:.96; }
blockquote{ color:var(--fg); border-left-color:var(--border); }
code,pre{ background:var(--code); color:var(--fg); }
.site-footer{ color:var(--muted); border-top:1px solid var(--border); }
</style>
